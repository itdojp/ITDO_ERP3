# Base URL for API gateway (pm-service proxy). Adjust per environment.
NEXT_PUBLIC_API_BASE=http://localhost:3001

# Optional: enable request logging in the browser console
NEXT_PUBLIC_ENABLE_API_LOGGING=true

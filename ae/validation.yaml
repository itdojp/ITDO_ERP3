ids:
  pattern: '^[A-Za-z0-9_-]+$'
dates:
  yyyy_mm: '^[0-9]{4}-[0-9]{2}$'
numbers:
  nonNegative: '>= 0'
rules:
  timesheets.hours: '>= 0'
  invoice_lines.tax_rate: '0..100'
patch:
  sales.lines_mode: ['replace','merge']
  procurement.lines_mode: ['replace','merge']
